import { createContext } from "react";


//WE can pass a default value to the create context Method as an argument
//We need to export context object to use.
export const ThemeContext = createContext("light");
